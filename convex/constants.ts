// Shared constants for the Convex backend

// Time threshold to consider a user online (in milliseconds)
export const ONLINE_THRESHOLD_MS = 2 * 60 * 1000; // 2 minutes

// Session expiry duration (in milliseconds)
export const SESSION_EXPIRY_MS = 30 * 24 * 60 * 60 * 1000; // 30 days

// Typing indicator timeout (in milliseconds)
export const TYPING_INDICATOR_EXPIRY_MS = 5000; // 5 seconds

// Message list default limit
export const DEFAULT_MESSAGE_LIMIT = 100;
